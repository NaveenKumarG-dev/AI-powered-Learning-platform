# Core runtime dependencies for the Vercel backend deployment.
# Heavy local media/ML packages are intentionally excluded because they exceed
# Vercel's Lambda storage limit and are only needed for the local Docker stack.
pillow
edge-tts
requests

# PostgreSQL driver
psycopg2-binary

# Supabase Python SDK (used for server-side helpers)
supabase

# Django ecosystem
djangorestframework
django-cors-headers
djangorestframework-simplejwt
django-filter

# Async task queue is disabled in the Vercel build.

# LangChain for AI course planning
langchain>=0.3.0
langchain-openai>=0.2.0
langchain-ollama>=0.2.0
langchain-core>=0.3.0
pydantic>=2.0.0
boto3>=1.26.0
